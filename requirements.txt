# TrojanLens starter — Apple Silicon (MPS) friendly deps.
# Install torch first (Apple Silicon default wheel already includes MPS):
#   pip install "torch>=2.2"
#
# NOTE: do NOT install bitsandbytes — it is CUDA-only and does not work on MPS.

torch>=2.2
transformers>=4.44
peft>=0.11
accelerate>=0.30
datasets>=2.19
captum>=0.7
scikit-learn>=1.3
numpy>=1.26
pyyaml>=6.0
tqdm>=4.66

# OPTIONAL — only for the quantized Qwen2.5-Coder-7B GGUF zero-shot baseline
# (Metal build). Not required for the toy pipeline or for LoRA training:
# llama-cpp-python>=0.2.90
